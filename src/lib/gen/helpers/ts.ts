import {to_root_path} from '../../path/paths.js';

export const renderTsHeaderAndFooter = (
	{originId}: {originId: string},
	content: string,
): string => {
	const originRootPath = to_root_path(originId);
	return `
		// generated by ${originRootPath}

		${content}

		// generated by ${originRootPath}
	`;
};
