{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "node",
			"request": "launch",
			"name": "gro",
			"program": "${workspaceFolder}/dist/cli/gro.js",
			"args": ["test", "resolve_specifier.test"],
			"runtimeArgs": [],
			// TODO need to add support sourcemaps to fix this, need to add back since we removed the build system
			// enables breakpoints in TS source
			"outFiles": ["${workspaceRoot}/dist/**/*.js"],
			"env": {
				"NODE_ENV": "development"
			}
		}
	]
}
